apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: netpol-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: nginx:1.31.3
          ports:
            - containerPort: 80
